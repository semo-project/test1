#writer-query
#adminWriterApporv
getListApprovCount=SELECT COUNT(*) FROM TB_WRITER WHERE APPROVAL_FLAG = 'N'
selectListWriterApprov=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.MEMBER_NO, MEMBER_NAME, MEMBER_ID, MEMBER_NICKNAME FROM TB_WRITER W JOIN TB_MEMBER M ON(W.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_FLAG = 'N' ORDER BY W.MEMBER_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getSearchApprovCount=SELECT COUNT(*) FROM TB_WRITER W JOIN TB_MEMBER M ON (W.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_FLAG = 'N' AND M.MEMBER_NAME LIKE ('%' || ? || '%')
searchApprovWriter=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.MEMBER_NO, MEMBER_NAME, MEMBER_ID, MEMBER_NICKNAME FROM TB_WRITER W JOIN TB_MEMBER M ON(W.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_FLAG = 'N' AND M.MEMBER_NAME LIKE ('%' || ? || '%') ORDER BY W.MEMBER_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getApprovWriter=SELECT W.MEMBER_NO, M.MEMBER_NICKNAME, W.REP_WORK_EXP, W.ORIGINAL_NAME, W.CHANGE_NAME FROM TB_WRITER W JOIN TB_MEMBER M ON(W.MEMBER_NO=M.MEMBER_NO) WHERE APPROVAL_FLAG = 'N' AND W.MEMBER_NO = ? 
#approveWriter=UPDATE TB_WRITER SET APPROVAL_FLAG='Y', APPROVAL_DATE = SYSDATE WHERE APPROVAL_FLAG='N' AND MEMBER_NO IN (?)

#adminWriter
getListCount=SELECT COUNT(*) FROM TB_WRITER WHERE APPROVAL_FLAG = 'Y' AND REST_FLAG = 'N'
selectWriter=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.MEMBER_NO, MEMBER_NAME, MEMBER_ID, MEMBER_NICKNAME, W.BANK_NAME, W.ACCOUNT_NO FROM TB_WRITER W JOIN TB_MEMBER M ON(W.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_FLAG = 'Y' AND REST_FLAG = 'N' ORDER BY W.MEMBER_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getSearchListCount=SELECT COUNT(*) FROM TB_WRITER W JOIN TB_MEMBER M USING (MEMBER_NO) WHERE APPROVAL_FLAG = 'Y' AND MEMBER_NAME LIKE ('%' || ? || '%')
selectSearchWriter=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.MEMBER_NO, MEMBER_NAME, MEMBER_ID, MEMBER_NICKNAME, BANK_NAME, ACCOUNT_NO FROM TB_WRITER W JOIN TB_MEMBER M ON(W.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_FLAG = 'Y' AND REST_FLAG = 'N' AND M.MEMBER_NAME LIKE ('%' || ? || '%') ORDER BY W.MEMBER_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
#restWriter=UPDATE TB_WRITER SET REST_FLAG = 'Y' WHERE MEMBER_NO IN (?)